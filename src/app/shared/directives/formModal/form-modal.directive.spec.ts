import { FormModalDirective } from './form-modal.directive';

describe('FormModalDirective', () => {
  it('should create an instance', () => {
    const directive = new FormModalDirective();
    expect(directive).toBeTruthy();
  });
});
